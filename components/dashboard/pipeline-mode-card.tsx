"use client";

/**
 * Mock/demo pipeline toggle has been retired.
 * Live doctrine pipeline is the only runtime path.
 * This stub remains so any stale imports fail closed without UI.
 */
export function PipelineModeCard(_props: { variant?: "operator" | "compact" }) {
  return null;
}
