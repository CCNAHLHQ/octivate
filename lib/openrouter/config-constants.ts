/** Stable defaults without importing the mutable model-config store (avoids cycles). */

/** OpenRouter slug — NVIDIA Nemotron 3 Super (free tier). Stable fallback. */
export const DEFAULT_OPENROUTER_MODEL = "nvidia/nemotron-3.5-lightning:free";
