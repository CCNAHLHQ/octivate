# Haraka config for Octivate outbound / local delivery
# Stub only — runtime mail is server/mail/local-smtp.mjs via: npm run mail:smtp
# (also auto-started by npm run serve:prod when MAIL_AUTO_START=true)

listen=127.0.0.1:2525
nodes=1
daemon_log_file=/dev/null
